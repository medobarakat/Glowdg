import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Gym = () => {
  return (
    <View>
      <Text>Gym</Text>
    </View>
  );
};

export default Gym;

const styles = StyleSheet.create({});
