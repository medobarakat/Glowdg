import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Caps = () => {
  return (
    <View>
      <Text>Caps</Text>
    </View>
  );
};

export default Caps;

const styles = StyleSheet.create({});
